{
  "name": "baseline-feature-advisor-action",
  "version": "1.0.0",
  "description": "GitHub Action for AI-Powered Baseline Feature Advisor",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist --source-map --license licenses.txt",
    "compile": "tsc",
    "package": "ncc build dist/index.js -o dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "web-features": "^0.8.6",
    "acorn": "^8.11.3",
    "acorn-walk": "^8.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  }
}